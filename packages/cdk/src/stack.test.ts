describe('BackEnd', () => {
  test('should behave...', () => {
    // Instantiate BackEnd and perform assertions
  });
});

describe('FrontEnd', () => {
  test('should behave...', () => {
    // Instantiate FrontEnd and perform assertions
  });
});

describe('Storage', () => {
  test('should behave...', () => {
    // Instantiate Storage and perform assertions
  });
});

describe('CloudTrailAlarms', () => {
  test('should behave...', () => {
    // Instantiate CloudTrailAlarms and perform assertions
  });
});

describe('MedplumStack', () => {
  test('should behave...', () => {
    // Instantiate MedplumStack and perform assertions
  });
});

describe('MedplumPrimaryStack', () => {
  test('should behave...', () => {
    // Instantiate MedplumPrimaryStack and perform assertions
  });
});

describe('MedplumGlobalStack', () => {
  test('should behave...', () => {
    // Instantiate MedplumGlobalStack and perform assertions
  });
});
